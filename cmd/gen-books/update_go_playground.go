package main

import (
	"fmt"
	"time"
)

// // ${baseDir} is books/go/
//
func updateGoPlaygroundLinks(baseDir string) {
	fmt.Printf("updateGoPlaygroundLinks() started\n")
	timeStart := time.Now()
	fmt.Printf("updateGoPlaygroundLinks() finished in %s\n", time.Since(timeStart))
}
